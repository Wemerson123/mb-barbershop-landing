/** Radius of the cursor spotlight, in CSS pixels. */
export const SPOTLIGHT_R = 260;

/** Size of one background grid cell, in CSS pixels. */
export const GRID_CELL = 48;

/** Easing factor for the raw pointer -> smoothed pointer chase. */
export const POINTER_EASE = 0.1;

/** Easing factor for the parallax grid drift. */
export const GRID_EASE = 0.06;

/** Maximum grid drift in pixels at the edges of the hero. */
export const GRID_DRIFT = 16;
